const setup = () => {
    let quote = 'onoorbaar';

    quote.slice(1,3)
    quote.slice(2,4)
    quote.slice(3,5)
    quote.slice(4,6)
    quote.slice(5,7)
    quote.slice(6,8)
    quote.slice(7,9)
}
console.log()

window.addEventListener("load", setup);